version: '3.8'

services:

  crud:
    build:
      context: ./CRUD
      dockerfile: Dockerfile
    ports:
      - "5001:8080"
    environment:
      ASPNETCORE_URLS: "http://+:8080"
      ConnectionStrings__DefaultConnection: "Server=host.docker.internal,1433;Database=TestDB;User Id=sa;Password=Nilu@123;TrustServerCertificate=True"

  apigetway:
    build:
      context: ./ApiGetWay
      dockerfile: Dockerfile
    ports:
      - "7000:8080"
    environment:
      ASPNETCORE_URLS: "http://+:8080"
    depends_on:
      - crud